<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>OpenUA.Online</title>
  <%= csrf_meta_tags %>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload'  %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body class='section-<%= @section %>'>
  <div class='ui container'>
    <nav class='ui secondary menu' style='margin-top: 1em;'>
      <%= link_to 'Головна',
                  root_path,
                  class: "item#{ ' active' if @section == 'home' }" %>

      <%= link_to 'Закупівлі',
                  tenders_path,
                  class: "item#{ ' active' if @section == 'tenders' }" %>

      <%= link_to 'Компанії',
                  companies_path,
                  class: "item#{ ' active' if @section == 'companies' }" %>

      <div class='item' style='width:50%;'>
        <form class='ui action left icon input'
              method='get'
              action='<%= @search_action_path %>'>
          <i class='search icon'></i>
          <input type='text'
                 name='search'
                 placeholder='<%= @search_placeholder %>'
                 value='<%= @search %>'>
          <button type='submit' class='ui button'>Пошук</button>
        </form>
      </div>
    </nav>
    <%= yield %>

    <aside class='ui center aligned basic segment'>
      <small class='muted'>Оновлено: <%= @date_modified.in_time_zone %></small>
    </aside>
  </div>
</body>
</html>
